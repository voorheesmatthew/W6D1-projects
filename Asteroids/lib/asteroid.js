const MovingObject = require("./moving_object.js");

function Asteroid (options) {
  this.color = "green";
  this.radius = 7;
  MovingObject.call({ pos: options[pos], vel: random, radius: this.radius, color: this.color})
  
  function
};

